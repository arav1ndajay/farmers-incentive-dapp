import React from "react";
import "../App.css";

export const Onboarding = () => {
  return (
    <div style={{ alignContent: "center", justifyContent: "center" }}>
      It seems like you do not logged in to metamask! Please do so in order to
      proceed!
    </div>
  );
};
